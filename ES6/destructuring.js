// let arr = [1, "ES6", false];

// let [id, name, free] = arr;

// console.log(id + " -" + name + "-" + free);

//Object

let objCourse = {
    id: 123,
    name: "ES6",
    free: false
}

let {id, name, free} = objCourse;

console.log(id + " -" + name + "-" + free);